/**
 * LocalConsolidator Stub for Workers Build
 * Real implementation available in local-consolidator.js.node (Node.js only)
 */
export class LocalConsolidator {
  constructor() {
    throw new Error('LocalConsolidator requires Node.js runtime (fs, path, os APIs). Use cross-session-sync Node.js server for local consolidation.');
  }
}
